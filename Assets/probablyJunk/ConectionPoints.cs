using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Experimental.AI;

public class ConectionPoints : MonoBehaviour
{
    public Vector3 BasicLenght =new Vector3 (-0.03f, 0, 0);
    public Vector3 Head;
    public Vector3 Tail;


    // Start is called before the first frame update
    void Start()
    {
        //Vector3 WorldEulerAngles = transform.eulerAngles;
        //Quaternion WorldAngel = transform.q
        //Vector3 Head = transform.position;
        //Vector3 ToTail = new Vector3();
        //for (int i = 0; i < 3; ++i)
        //{
        //    ToTail[i] = Mathf.Cos(WorldEulerAngles[i]) * BasicLenght[i];
        //}
        //Vector3 Tail = Head + ToTail;
        //Debug.Log(Tail);
    }

    // Update is called once per frame
    void Update()
    {
        //Vector3 WorldEulerAngles = transform.eulerAngles;
        //Vector3 Head = transform.position;
        //Vector3 ToTail = new Vector3();
        //for (int i = 0; i < 3; ++i)
        //{
        //    ToTail[i] = Mathf.Cos(WorldEulerAngles[i]) * BasicLenght[i];
        //}
        //Vector3 Tail = Head + ToTail;
        //Debug.Log(Tail);
    }
}
